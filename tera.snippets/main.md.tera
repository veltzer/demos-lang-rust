## Number of examples

Currently there are {{ git_count_files(pattern="examples/**/*.rs") }} examples in this repo.
Currently there are {{ git_count_files(pattern="exercises/**/*.rs") }} exercises in this repo.
