#![allow(unused)]

// This is an example of an unused variabe.
// If you disable the compiler directive above you will get a compilation error.
// another option is to call the variable _* which allows it to be unused (similar to python).

fn main() {
    let _x: i32=0;
    let x: i32=0;
    println!("Hello World!");
}
